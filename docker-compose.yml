services:
  mongodb:
    image: mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: secret
    volumes:
      - user:/data/db

  # auth:
  #   build:
  #     context: ./auth-service
  #     target: development
  #   ports:
  #     - '3001:3001'
  #   volumes:
  #     - ./auth-service:/app:ro
  #   env_file:
  #     - './auth-service/.env'
  #   depends_on:
  #     - mongodb
volumes:
  user: